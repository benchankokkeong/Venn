<resources>

    <!--
        Base application theme for API 21+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 21+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Material">
        <!-- API 21 theme customizations can go here. -->
        <item name="android:colorPrimaryDark">@color/federation_statusbar_blue</item>
    </style>

    <style name="MainFloatingActionButton" parent="android:Widget.Material.Button">
        <item name="android:background">@drawable/main_fab_background</item>
        <item name="android:stateListAnimator">@anim/fab_selector</item>
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
    </style>

    <style name="FloatingActionButton" parent="android:Widget.Material.Button">
        <item name="android:background">@drawable/floating_action_button_background</item>
        <item name="android:stateListAnimator">@anim/fab_selector</item>
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
    </style>

    <style name="Toolbar" parent="android:Widget.Material.PopupMenu.Overflow">
        <item name="android:overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">50dip</item>
        <item name="android:popupBackground">@color/federation_white</item>

        <item name="android:actionMenuTextColor">@color/federation_black</item>
        <item name="android:textColor">@color/federation_black</item>
        <item name="android:textColorPrimary">@color/federation_black</item>
    </style>

</resources>